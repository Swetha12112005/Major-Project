# -*- coding: utf-8 -*-
"""Eda-Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/13QCTUE3IQhty7rTRz1ajKtbj1X5OQU0_

**Introduction**

Exploratory Data Analysis (EDA) is a crucial step in the data analytics process. It helps in understanding the dataset, identifying patterns, detecting anomalies, handling missing values, and preparing the data for further analysis or machine learning.


This project performs a complete, well-structured, and academic-level EDA on a Student Placement Dataset, focusing on factors that influence student placement outcomes.

**Objective of the Project**

The main objectives of this project are:

To understand the structure and nature of the placement dataset

To clean and preprocess the data

To analyze relationships between academic, skill-based, and placement variables

To visualize insights using appropriate graphs

To derive meaningful conclusions for decision-making

**Dataset Description**

The dataset contains information about students from different departments and their placement status.

📌 Attributes Description
Column Name	Description
student_id	Unique ID of student
department	Branch of study (CSE, IT, ECE, etc.)
cgpa	Cumulative Grade Point Average
active_backlogs	Number of active backlogs
skill_stack	Student skill domain
internships	Number of internships completed
projects	Number of projects done
aptitude_score	Aptitude test score
communication_score	Communication skill rating
placed	Placement status (1 = Placed, 0 = Not Placed)

4. Importing Required Libraries
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import warnings
warnings.filterwarnings('ignore')

"""5. Loading the Dataset"""

df=pd.read_excel('/content/placement_data.xlsx')

df.head()

df.tail()

"""6. Basic Data Understanding

6.1 Shape of Dataset

"""

df.shape

df.info()

df.describe()

"""Insight:

Numerical features show reasonable ranges

CGPA ranges approximately between 5 and 10

Aptitude and communication scores vary significantly

7. Data Cleaning

7.1 Checking Missing Values
"""

df.isnull().sum()

"""7.2 Handling Missing Values

Numerical columns are filled with mean

Categorical columns are filled with mode
"""

df['cgpa'].fillna(df['cgpa'].mean(), inplace=True)
df['communication_score'].fillna(df['communication_score'].mean(), inplace=True)
df

"""Understanding the Target Variable (Placed)"""

df['placed'].value_counts()

df['placed'].value_counts().plot(kind='bar')
plt.title("Placement Status Distribution")
plt.xlabel("Placed (0 = No, 1 = Yes)")
plt.ylabel("Number of Students")
plt.show()

"""Univariate Analysis

🔹 CGPA Distribution
"""

sns.histplot(df['cgpa'], kde=True)
plt.title("CGPA Distribution")
plt.show()

"""Aptitude Score Distribution"""

sns.histplot(df['aptitude_score'], kde=True)
plt.title("Aptitude Score Distribution")
plt.show()

"""Department-wise Student Count"""

sns.countplot(x='department', data=df)
plt.title("Students by Department")
plt.show()

"""Placement Status Distribution"""

sns.countplot(x='placed', data=df)
plt.title('Placement Status Distribution')
plt.show()

"""Bivariate Analysis

 CGPA vs Placement
"""

sns.boxplot(x='placed', y='cgpa', data=df)
plt.title('CGPA vs Placement')
plt.show()

"""Aptitude Score vs Placement"""

sns.boxplot(x='placed', y='aptitude_score', data=df)
plt.title('Aptitude Score vs Placement')
plt.show()

"""Backlogs vs Placement"""

sns.countplot(x='active_backlogs', hue='placed', data=df)
plt.title('Backlogs vs Placement')
plt.show

"""Multivariate Analysis

Correlation Heatmap
"""

plt.figure(figsize=(10,6))
sns.heatmap(df.select_dtypes(include=np.number).corr(), annot=True, cmap='coolwarm')
plt.title("Correlation Heatmap")
plt.show()

"""Step 4: Univariate Analysis (Individual Features)
Analyzing one variable at a time to understand distributions.

4.1 Target Variable Distribution (Placed vs Not Placed)
Check if the dataset is balanced.
"""

plt.figure(figsize=(6, 5))
ax = sns.countplot(x='placed', data=df, palette='viridis')
plt.title('Distribution of Placement Status')
plt.xticks([0, 1], ['Not Placed', 'Placed'])

# Add percentage labels on top of bars
total = len(df)
for p in ax.patches:
    percentage = '{:.1f}%'.format(100 * p.get_height() / total)
    x = p.get_x() + p.get_width() / 2
    y = p.get_height()
    ax.annotate(percentage, (x, y), ha='center', va='bottom')

plt.show()

""" Distribution of CGPA
Understanding the academic performance of students.

python


"""

plt.figure(figsize=(8, 5))
sns.histplot(df['cgpa'], kde=True, bins=20, color='skyblue')
plt.title('Distribution of CGPA')
plt.xlabel('CGPA')
plt.ylabel('Frequency')
plt.axvline(df['cgpa'].mean(), color='red', linestyle='--', label=f"Mean CGPA: {df['cgpa'].mean():.2f}")
plt.legend()
plt.show()

"""Skill Stack vs Placement
Which tech stacks are in demand?
"""

plt.figure(figsize=(10, 6))
sns.countplot(x='skill_stack', hue='placed', data=df, palette='deep')
plt.title('Placement Status by Skill Stack')
plt.legend(title='Status', labels=['Not Placed', 'Placed'])
plt.show()

"""CGPA vs Aptitude Score (Colored by Placement)
Scatter plot to see clusters.
"""

plt.figure(figsize=(10, 6))
sns.scatterplot(x='cgpa', y='aptitude_score', hue='placed', style='placed', s=100, data=df, palette='Set1')
plt.title('CGPA vs Aptitude Score (Cluster Analysis)')
plt.axhline(df['aptitude_score'].mean(), color='gray', linestyle='--')
plt.axvline(df['cgpa'].mean(), color='gray', linestyle='--')
plt.show()

"""Bivariate Analysis (Feature vs. Target)
Analyzing relationships between two variables (specifically, how features impact placement).

5.1 CGPA vs Placement (Boxplot)
Does higher CGPA guarantee placement?
"""

plt.figure(figsize=(8, 6))
sns.boxplot(x='placed', y='cgpa', data=df, palette='Set2')
plt.title('CGPA vs Placement Status')
plt.xticks([0, 1], ['Not Placed', 'Placed'])
plt.show()

"""Key Findings

Academic performance strongly impacts placement

Soft skills and aptitude play a major role

Students with internships and projects have better chances

Backlogs negatively affect employability

Conclusion

This EDA project provided a comprehensive understanding of the student placement dataset. Through data cleaning, visualization, and statistical analysis, we identified key factors influencing placement outcomes. These insights can be used for career guidance, institutional planning, and predictive modeling
"""